-- =============================================================================
-- CONFIGURATION
-- =============================================================================
local Config = {
    -- Dot appearance
    DOT_SIZE = 10,
    PULSE_SPEED = 0.3,
    PULSE_MIN_RATIO = 0.1,
    
    -- Movement parameters
    DOT_SPEED = 500,
    CIRCLE_RADIUS = 100,
    CIRCLE_DURATION = 3,
    TRANSITION_DURATION = 0.5,
    
    -- Movement weights
    CIRCLE_WEIGHT = 0.7,
    LINE_WEIGHT = 0.3
}

return Config 
